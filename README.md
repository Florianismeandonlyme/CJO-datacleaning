# CJO-datacleaning
## Ipynb File
Run the Jupyter notebook to **split** a large judgment textual file into several sub-files.
## do File
Run the do File to **collapse** the sub-files into a region-year panel data in **dta** format.
